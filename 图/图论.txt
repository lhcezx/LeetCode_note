Dijkstra算法的核心是，我们用一个dp数组记录每一个遍历到的节点时的路径和的最小值，比如节点3当我们从12走到3时有一个路径和，从54走到3也有一个路径和，保存最小的，然后我们每次遍历到一个节点，我们判断当前路径和和之前保存的最小和，然后根据需求来判断是否跳过这条路径。
同时我们需要邻接表或邻接矩阵或者能表示节点间相邻关系的一个数据结构，假如我们当前路径和最小，那么我们遍历该节点的邻居节点，同时判断是否到邻居节点的路径是最小的，如果是则更新dp数组，并将节点放入优先队列中。优先队列的作用是将堆顶的元素取出，例如我们找路径和最大的路径，那么我们优先选择现有路径中路径和最大的，贪心思路。